#include <iostream>
#include <string>

int main(int argc, char **argv)
{
  // int64_t arg_first;
  // int64_t arg_second;

  // std::cin.tie(nullptr);
  // std::cin.sync_with_stdio(false);

  // std::cout.tie(nullptr);
  // std::cout.sync_with_stdio(false);

  // while (std::cin >> arg_first >> arg_second) {
  //   std::cout << (arg_first + arg_second) << "\n";
  //   std::cout.flush();
  // }
  printf("YOLO");

  return 0;
}
